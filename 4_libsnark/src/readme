要在CMakeLists.txt裡面加上
```
add_executable(
  test_program

  test.cpp
)

target_link_libraries(
  test_program

  snark
)

target_include_directories(
  test_program

  PUBLIC
  ${DEPENDS_DIR}/libsnark
  ${DEPENDS_DIR}/libsnark/depends/libfqfft
)
```
然後在這個資料夾裡面的 src 執行 cmake ..
最後 make

然後最後編譯出來的程式會在src裡面
./[name]
出來如果是 1 表示驗證成功 0 表示驗證失敗

注意: 這個資料夾外面的東西都不能刪除 那些都是dependency